# sash-relay (unmanned bus)

Unmanned relay channel for SaSH Client05 porting.
- Codex (PC-A) pushes: RESULT.md, out/ logs, artifacts/ screenshots and .map
- Claude (cloud) pulls anonymously; public repo, no token needed
- NO secrets committed (see .gitignore). PersonalKey / tokens never here.
